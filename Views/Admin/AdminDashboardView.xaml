<UserControl x:Class="CAKA.PerformanceApp.Views.Admin.AdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <Grid Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="Toplam Çalışan" Style="{StaticResource SubtitleTextStyle}" Margin="0,0,0,8"/>
                    <TextBlock FontSize="32" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}">
                        <TextBlock.Text>
                            <Binding Path="TotalEmployees"/>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="Haftalık Toplam Saat" Style="{StaticResource SubtitleTextStyle}" Margin="0,0,0,8"/>
                    <TextBlock FontSize="32" FontWeight="Bold" Foreground="{StaticResource AccentBrush}">
                        <TextBlock.Text>
                            <Binding Path="WeeklyTotalHours" StringFormat="{}{0:N1}"/>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Bu hafta çalışan başına toplam saat" Style="{StaticResource TitleTextStyle}" Margin="0,0,0,16"/>
                <ItemsControl ItemsSource="{Binding ChartData}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" MinWidth="80"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Label}" Style="{StaticResource SubtitleTextStyle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <Border Grid.Column="1" Height="24" CornerRadius="4" Margin="8,0,8,0" HorizontalAlignment="Left" MaxWidth="320"
                                        Width="{Binding Value, Converter={StaticResource ChartBarWidthConverter}}"
                                        Background="{StaticResource AccentBrush}" Opacity="0.85"/>
                                <TextBlock Grid.Column="2" Text="{Binding Value, StringFormat='{}{0:N1} sa'}" Style="{StaticResource SubtitleTextStyle}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Son aktiviteler" Style="{StaticResource TitleTextStyle}" Margin="0,0,0,16"/>
                <ItemsControl ItemsSource="{Binding RecentActivities}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="0,10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding Description}" Style="{StaticResource SubtitleTextStyle}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding Time, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" Style="{StaticResource SubtitleTextStyle}" VerticalAlignment="Center" Margin="24,0,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
