<Window x:Class="CAKA.PerformanceApp.Views.Admin.AdminShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:admin="clr-namespace:CAKA.PerformanceApp.ViewModels.Admin"
        xmlns:views="clr-namespace:CAKA.PerformanceApp.Views.Admin"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CAKA - Yönetici Paneli"
        Height="700" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <DataTemplate DataType="{x:Type admin:AdminDashboardViewModel}">
            <views:AdminDashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type admin:AdminEmployeesViewModel}">
            <views:AdminEmployeesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type admin:AdminReportsViewModel}">
            <views:AdminReportsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type admin:AdminSettingsViewModel}">
            <views:AdminSettingsView/>
        </DataTemplate>
        <ControlTemplate x:Key="SidebarButtonTemplate" TargetType="Button">
            <Border x:Name="bg" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"
                    CornerRadius="6">
                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="bg" Property="Background" Value="#22FFFFFF"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}" CornerRadius="0">
            <StackPanel Margin="0,24,0,0">
                <TextBlock Text="CAKA" Foreground="White" FontSize="20" FontWeight="Bold" Margin="24,0,0,32"/>
                <ItemsControl ItemsSource="{Binding MenuItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Title}"
                                    Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent" Foreground="White" BorderThickness="0"
                                    HorizontalContentAlignment="Left" Padding="24,14" Margin="8,2"
                                    Cursor="Hand" FontSize="14"
                                    Template="{StaticResource SidebarButtonTemplate}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Border Height="1" Background="#33FFFFFF" Margin="16,24,16,16"/>
                <Button Content="Çıkış" Command="{Binding LogoutCommand}"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        HorizontalContentAlignment="Left" Padding="24,14" Margin="8,2"
                        Cursor="Hand" FontSize="14"
                        Template="{StaticResource SidebarButtonTemplate}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{StaticResource CardBackgroundBrush}" Padding="24,16">
                <Grid>
                    <TextBlock Text="{Binding PageTitle}" Style="{StaticResource TitleTextStyle}" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding UserDisplayName}" Style="{StaticResource SubtitleTextStyle}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            <ScrollViewer x:Name="ContentScrollViewer" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
                <ContentControl Content="{Binding CurrentPage}"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
