<UserControl x:Class="CAKA.PerformanceApp.Views.Personel.PersonelDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="350" d:DesignWidth="500">
    <StackPanel>
        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Haftalık toplam saat" Style="{StaticResource SubtitleTextStyle}" Margin="0,0,0,8"/>
                <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}">
                    <TextBlock.Text>
                        <Binding Path="WeeklyTotalHours" StringFormat="{}{0:N1} saat"/>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </Border>
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Son 10 aktivite" Style="{StaticResource TitleTextStyle}" Margin="0,0,0,16"/>
                <ItemsControl ItemsSource="{Binding RecentWorkLogs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="0,10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Margin="0,0,0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Description}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Date, StringFormat='{}{0:dd.MM.yyyy}'}" Style="{StaticResource SubtitleTextStyle}" Margin="16,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,0,0">
                                        <TextBlock.Text>
                                            <Binding Path="Hours" StringFormat="{}{0:N1} sa"/>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
